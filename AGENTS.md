# CLAUDE.md

## 環境
- Python / uv のみ。pip でグローバルに入れない。

## 実装時に気をつけること
- logを取るように。/logsディレクトリを作成し、詳細なログファイルを吐く実装とし、実行=>ログのチェック=>改善=>実行のサイクルを繰り返すこと
- 1コードの責任が重くなりすぎた場合は適宜責任を分離、リファクタリングする

## やってはいけないこと
- `npm`  `java` `gcc` など未導入のツールを使わない
- 設定ファイルを無断で上書きしない
- ログやprintに絵文字を使わない。一般的な日本語、英語のみ。
- batファイルに日本語を書かない

## Gitについて
- 1ターンごとにコミットすること.コミットメッセージはわかりやすく日本語でつけて。
- 変更を取り消す,もとに戻して等といった場合は,gitを介して戻ること.
- 機能追加や方針変更などはブランチを切ること。なお同時にREADME.mdを更新し実際の実装と内容を合わせること